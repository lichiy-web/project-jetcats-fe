import Logo from '../Logo/Logo';
import UserMenu from '../UserMenu/UserMenu';

const Header = () => {
  return (
    <header>
      <Logo />
      <UserMenu />
    </header>
  );
};

export default Header;
